spring.application.name=fleet-flow-backend



spring.datasource.url=jdbc:postgresql://localhost:5432/fleet_flow
spring.datasource.username=postgres
spring.datasource.password=argusadmin
spring.jpa.hibernate.ddl-auto=update
server.port=9090

# JWT (dev default; change for production)
app.security.jwt.secret=fleet-flow-dev-secret-key-32bytes-minimum!!
app.security.jwt.issuer=fleet-flow
app.security.jwt.ttl-seconds=3600

# CORS
app.security.cors.allowed-origins=http://localhost:5173